from servermedium.django.server import SiteServer, SiteServerConfig
import servermedium.django.filters as f

config = SiteServerConfig()
                            


SiteServer(config).runserver(globals())
